# 漫镜 v1.5.1

v1.5.1 补齐欧美动画生产风格，并增强英文剧本导入和结构识别。

## 欧美动画风格

- 新增“欧美电影写实”：锁定多肤色自然还原、实景制作、动机光、35mm 质感与克制电影调色。
- 新增“欧美剧集写实”：锁定现代生活空间、实用光源、稳定剧集 LUT、生活化调度与高级流媒体质感。
- 新增“欧美二维动画”：锁定线稿层、角色色板、剪影、关键姿势和跨帧色块稳定性。
- 新增“欧美院线动画”：锁定风格化三维造型、材质复杂度、角色专属色彩脚本和电影级灯光。
- 新增“欧美成人动画”：采用成熟角色比例、克制色板、经济线条和精准表演节奏。
- 新增“欧美3D动漫”：采用欧美剧集式风格化 CGI、可绑定角色比例、稳定表情拓扑、toon/PBR 混合材质与动作冒险镜头语言；明确排除图像小说的墨线、网点和印刷纹理。
- 为六种风格增加独立预览图，并在角色标准图、分镜、视频动态和风格质检链路中加入防真人化、防日漫化、防国漫化约束。

## 英文剧本导入

- 支持 `EPISODE 1`、`EP. 2`、`CHAPTER 3` 和 `PART 4` 等英文分集标题。
- 支持标准 `INT.`、`EXT.` 场景标题，并避免将场景、转场和镜头指令误识别为角色。
- 支持全大写角色提示、冒号对白、`V.O.`、`O.S.` 和 `CONT'D` 等英文剧本标记。
- 英文剧本拆镜时保留英文角色名与对白，不再强制翻译成中文。
- 视频口型提示会根据对白自动选择 English 或 Simplified Chinese。
- 英文背景、人物关系和连续性规则可进入系列项目记忆。

## 逐项审核

- 新增非阻塞逐项审核队列：角色图、镜头画面、视频、配音和配乐一生成即可分别预览、批准或删除，其他岗位继续生产。
- 所有待审项处理完成后才允许合成成片，未经批准的视频不会提前进入成片或资产库。
- 移除首帧质检的同步确认弹窗；未通过自动检查的结果会直接进入审核队列，不再要求等全部资产完成后统一处理。

## 人物资产命名

- 人物基础资产直接使用人物名，例如“林辰”。
- 系统会从服装、造型和人物状态中识别变体，并命名为“人物名＋变体”，例如“林辰西装版”“林辰颓废版”“林辰富有版”“林辰运动版”。
- 同一人物的不同造型共享人物身份键，但拥有独立造型键，可以并存、分别设为 Canonical 并被 Agent 精确复用。
- 在资产库中手动修改人物名字或服装/状态时，资产标题会同步更新。

## 剧本资产框架

- 导入 TXT、Markdown 或 JSON 剧本后，AI 会先通读全文并拆出需要稳定视觉身份的人物与剧情重要道具。
- 分析阶段只创建可编辑的空资产卡，不调用生图模型，也不会自动消耗生图额度。
- 每个人物和道具都可以分别上传用户已有图片，或由用户点击“让 AI 生成”；AI 结果生成后立即单项审核。
- 资产库中的人物与道具会优先按名称匹配空卡，避免已有资产被当作普通场景素材。
- 工程与分镜 JSON 会保存人物/道具框架、描述、重要性和远程参考；恢复工程后可继续补齐。
- 正式生成视频前会检查所有人物和道具资产卡；缺图或待审核项会被明确列出，不会先生成分镜图绕过资产选择。

## Canonical 场景资产

- 剧本分析会同时提取本集需要稳定复用的地点、布景、时段、天气和场景状态，并先建立不含图片的场景资产框架。
- 场景资产是没有人物和可移动剧情道具的 Canonical 空场景参考，不是逐镜分镜图。
- 用户可以上传已有场景图，或让生图 AI 生成；支持预览、单项审核、替换和一键补齐，采用后自动进入资产库。
- 分镜必须沿用预分析阶段的 `environmentKey`；视频生成时按键精确引用对应场景图。
- 即使完全不用首帧、尾帧和分镜图，Seedance 仍会组合人物造型、Canonical 场景、道具与音色的全能参考。

## 人物音色库

- 每个人物第一次在生成视频中说话后，会从该镜头摘取参考音轨并写入音色库；本地桥接可输出标准 MP3，浏览器解码回退会保存 WAV。
- 首条视频对白提取后，本集后续镜头立即作为全模态音色参考使用；跨项目复用仍需用户在音色库确认授权。
- 音色资产记录人物身份、参考台词、来源、授权状态、引用次数和 Canonical 状态，可试听、替换、撤销授权或禁止 Agent 复用。
- CosyVoice 本地桥接支持 `reference_audio + referenceText` 的 zero-shot 克隆；自定义配音 Webhook 同样会收到结构化音频参考。
- 本地桥接新增单人物对白视频的 MP3 摘取、降噪和响度标准化接口；多人混音不会冒充自动识别，需先完成说话人分离。
- 未确认授权或已撤销授权的声音不能设为 Canonical，避免误用真人声音。
- 音色库现已从视觉资产库拆分为独立页面，按项目/剧本分组，并提供所有项目可复用的公共音色库。
- 导入剧本时会为实际有对白的人物建立空音色框架；用户可以逐项上传授权音频，或点击调用配音 AI 生成参考音色。

## Seedance 全能参考与长视频连续性

- 每次视频生成前先执行资产库预检，优先命中当前项目的 Canonical 人物/造型、场景、道具和音色，再补充公共资产。
- Seedance 2.0 按 `@Image`、`@Video`、`@Audio` 建立逐项用途绑定，单任务最多组织 9 张图片、3 个视频、3 个音频且总数不超过 12。
- 原生视频模型不再生成逐镜分镜图或预制首帧；人物、Canonical 空场景和道具资产就绪后，首镜直接使用 Seedance 全能参考生成。
- 系统根据同场景、共同人物、动作/视线/位置承接等条件判断是否需要尾帧；只有需要跨镜衔接的尾帧才作为资产保存。
- 系列项目会额外保存每集最后一镜尾帧；从第二集开始，首镜自动查找并引用上一集尾帧。
- Seedance 已生成对白时不再叠加独立 TTS，避免双重人声和口型冲突。
- 每镜继续锁定 environmentKey、场景圣经、人物站位、道具归属、色调和光线；最终合成使用补帧式柔和过渡或干净硬切，避免机械拼接 15 秒片段。
