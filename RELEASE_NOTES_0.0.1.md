# 漫镜 v0.0.1

首个公开测试版本，面向 Windows 10/11 x64。

![漫镜工作台](https://raw.githubusercontent.com/Laity1m/manjing-desktop/main/README-media/漫镜视觉-01.png)

## 下载

下载 Release Assets 中的 `Manjing-0.0.1-Windows-x64.exe`。

## 本版本重点

- 总剧本一次性分析，首次清单成为后续岗位唯一资产依据。
- 当前项目和公共资产优先匹配，已有图片不会再次生成。
- 同一人物不同服装/状态独立建模并复用同一身份基准。
- 项目间人物、视频、音色和可信人物阻断相互隔离。
- 逐镜批准后从实际生成视频解码真实尾帧，据此生成过肩、侧面和反打等三组不同景别/角度备选；用户选定后才生成下一镜。
- 原始尾帧不直接充当下一镜构图，Seedance 只接收用户选中的多机位画面作为普通 `@Image` 全能参考，不启用 `first_frame/last_frame` 控制。
- 视频逐镜生成、预览、审核；支持填写修改要求后重新生成。
- 首次角色对白可提取为项目专属声音参考，后续镜头优先引用。
- 项目长期记忆、分集上下文与已批准镜头事件账本共同参与后续制作。
- 所有文本 Agent 通道统一检索 Skill，并在制作记录中显示实际调用项。
- 新增半自动制作模式：展示并允许修改最终 Seedance 提示词、引用素材和提交参数，用户确认后才发起生成。
- 默认岗位知识新增原创角色捏脸、全能参考规划、动作预算、工程化提示词、局部修复和剪辑衔接等六组精炼 Skill。
- Skill 会按当前岗位和具体任务提高检索优先级，确保知识不只存在于配置页，而会真正注入导演、分镜、提示词、生图、视频与剪辑流程。
- 修复人物位置漂移相关的空间锚点、构图边界和冻结模式约束。
- 2.5D 顶视舞台默认只显示未生效草稿；用户确认后才把人物、固定场景物体与相机坐标提交给视频模型。
- 普通服装、容器、餐具和手持道具不再被误判为固定舞台物；旧版自动生成的错误物体坐标会安全清理。
- 固定物支持地图节点单独删除、列表删除和一键清空；同场景布局仅在用户主动操作后同步。
- 欧美 2D 动画、欧美 3D 动漫、欧美动画电影与欧美写实等默认视觉方向。

## 重要说明

- v0.0.1 是测试版本，请先用短剧本验证模型配置和成本。
- 第三方生成模型具有随机性；一致性检查会降低漂移，但不能绝对替代传统 3D 绑定。
- 真人写实参考可能需要服务商可信人物授权；动画项目不会要求沿用其他项目的真人授权。
- API Key 由用户自行配置，不包含在安装包和仓库中。

## 验证范围

- TypeScript 类型检查
- 单元与流程回归测试
- Web 生产构建
- Electron 启动自检
- Windows x64 NSIS 安装包构建
- 151 项自动化测试全部通过

安装包 SHA256：`E3A74D70ED9BE9FDA219EB807C150BC10798BC4D1A823DD9CEFE892470ACEA47`

问题反馈：[GitHub Issues](https://github.com/Laity1m/manjing-desktop/issues)
