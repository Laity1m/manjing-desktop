# 漫镜 v1.5.0

> 当前 v1.5.0 安装包已合入审核流程热修复：总分达到 90 自动进入视频生成；低于 90 时由用户选择重做、人工继续或设为可复用后继续，不变更版本号。

v1.5.0 是一次围绕系列项目、Agent 能力、资产一致性和使用流畅度的综合升级。

## 性能重构

- 188 个默认 Skill 改为浏览器空闲时异步导入。
- Skill 与记忆读取增加内存缓存，减少重复 JSON 解析和主线程阻塞。
- 重页面不再由顶部导航提前预取，只加载用户当前打开的功能。
- 改为单窗口客户端路由，不再运行第二个完整 Electron 页面，降低切页内存与 CPU 峰值。
- 新增统一的页面加载反馈。

## Agent 与 Skill

- 新增镜头总控 Agent，负责资产调用、状态继承和视频提示词整合。
- 生图 Agent 支持独立聊天、记忆和 Skill 导入。
- 默认岗位 Skill 共 188 个，按导演、编剧、分镜、生图、视频、配音和剪辑等岗位分类。
- 总制片可向各岗位派送技能和长期记忆。

## 系列项目

- 支持导入整部总剧本并拆分剧集。
- 项目级角色圣经、背景故事、连续性规则、时间线和资产隔离。
- AI 工作台支持绑定和切换项目、剧集。

## Canonical 一致性系统

- 人物、造型、场景、道具和声音支持标准资产锁定与复用。
- 场景图不再自行重新设计已锁定道具。
- 镜头间传递 Start State / End State。
- 审核只检查当前景别中应当可见的内容，画外房门、床、下装等不再错误扣分。
- 严格限制无依据新增耳饰、眼镜、纹身等身份特征。
- 总分低于 90 分时进入人工复核，由用户决定重做、继续生成或将当前首帧标记为可复用。
- 人物、场景、道具、分镜与声音新增稳定复用键；完全匹配时直接使用原文件，不再重复调用模型或新增重复资产。

## 视频与声音

- Seedance 创建请求采用谨慎重试策略，并保存漫镜请求编号。
- 查询和下载任务支持安全重试。
- 默认 720p，并记住上次分辨率设置。
- 配音与背景音乐开关彼此独立，角色声音可作为项目资产复用。
- 新增独立字幕总开关，覆盖预览、时间轴、编辑器交接和最终视频烧录。
- 核心按钮、设置卡片和状态切换加入轻量动效、悬停反馈、移动端自适应及减少动画辅助功能。

### 原生视频工作流修补

- 原生视频模型跳过强制分镜图步骤，逐镜直接生成视频。
- 视频生产首帧仅作为内部生成与质检条件；生产首帧、视频中间帧和镜尾帧不再归档为用户可见的分镜图片资产。
- 人物标准图改为身份与服装解耦布局：左侧大头照强制平视正脸并作为唯一身份锚点，右侧三视图只用于服装和体型，正面与侧面五官区域自动遮罩。
- 修复本地人物参考图没有真正传入生图模型的问题；OpenAI 兼容接口有参考图时改用图片编辑请求，缺少出镜人物参考图时安全停止，避免换脸、换装和重新设计人物。
- 视频完成后自动截取关键帧与镜尾帧，用于资产归档和下一镜连续性。
- Canonical 人物脸部、服装、道具、场景、画面位置和上一镜状态进入镜头总控约束。
- 单镜头失败不再中断后续镜头生成。
- 旁白、广告声、系统音和画外音不再生成人物图。
- 资产库新增常驻批量选择与批量删除入口。

### 漫剧工业化流程重构

- 工作台明确采用“项目圣经 → 剧集拆解 → 资产规划 → 镜头设计 → 镜头生产 → 连续性审核 → 声音后期 → 剪辑交付”八阶段流程。
- 镜头计划、视觉参考和长期资产分层管理，不再把所有生成图片都当作 Canonical 资产。
- 视频 Agent 会按上一镜尾帧、角色身份与造型、地点布局、关键道具、角色声音的优先级检索参考。
- 旁白、广告声、系统播报与画外音保留声音身份，但不进入人物视觉资产生成。
- 工作台新增可视化生产流程地图，明确每个 Agent 的交接位置和成果。
- 默认技能库新增“原创AI角色捏脸提示词生成 Skill”，归属生图 Agent，并实际参与首次角色建模与角色资产补跑。

### 精细化漫剧 SOP 与启动修复

- 导演、镜头总控和剪辑 Agent 新增人物统一、画风统一、动作咬合、轴线空间、转场、色彩与声音交付规则。
- 同场对话、连续动作、普通延续和正式换场采用不同衔接策略；镜尾与下一镜开头传递约 0.5 秒承接姿态。
- 新增响应式“本片生产标准”控制台，实时显示画风、画幅、时长、连续性、配音、BGM 和字幕状态。
- 修复生产标准面板引用错误变量造成 `/studio` SSR 返回 500、Windows 安装版提示“内置应用启动自检失败”的问题。
- Windows 内置应用启动自检改为验证真实 HTML 状态、响应类型和页面结构，不再依赖页面正文固定文字。

### 空间状态锁与跨镜头几何连续性

- 为每个镜头编译人物世界锚点、左右顺序、景深、朝向、姿态、出入画方向及道具归属约束。
- 生产首帧与上一镜合格尾帧作为几何依据，End State 自动传给下一镜 Start State。
- 摄像机变化时保持世界位置并重新投影，连续对话遵守 180 度镜头轴线，避免人物和道具无理由换边。
- 禁止人物或物体凭空生成、消失、复制、换手和瞬移；进出画必须符合画面边缘、门口或遮挡关系。
- 总分达到 90 分自动进入视频生成；单项偏差作为审核提示展示，低分镜头由用户决定是否继续或复用。

### 语义资产身份卡

- AI 生成资产自动保存所属项目、实体名称、造型版本、引用用途、识别置信度和复用范围。
- 人物设定图标记脸部、正面、侧面和背面区域，Agent 会根据近景、侧面、背影或全身镜头选择对应参考区域。
- 场景资产记录布局、灯光、风格和空间锚点；道具资产记录外形、材质、归属和位置用途；声音资产绑定角色声音身份。
- 用户上传但尚未确认的素材标记为待识别，避免被当作 Canonical 标准资产盲目调用。
- 资产检索先按项目、剧集、实体、造型、场景和用途匹配，再进行语义排序。

### 风格锁定与候选复核

- 新增“半写实3D国漫”候选，明确动漫五官、细腻 CG 建模、真实发丝服饰质感与非真人化边界。
- 国漫电影感调整为写意国漫电影和绘制感 CG，避免同一选项在真人写实、半写实 3D 和二维漫画之间漂移。
- 原生视频生成前先建立并审核生产首帧；失败视频支持按问题重生成、人工批准或删除候选。
