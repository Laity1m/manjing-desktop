# 漫镜 v1.5.2

v1.5.2 修复资产库已有图片未被生图 Agent 识别、同一人物重复建卡和重复生图的问题。

## 已有资产优先复用

- 剧本资产分析建立人物、场景和道具框架时，会先按身份键和造型键检索已有成品资产。
- 支持检索当前项目、公共资产以及同一资产库中的旧项目/旧集资产；当前项目资产优先。
- 人物名匹配不再区分大小写，并统一识别“基础版、基础造型、Base Look、Default Look”等同义造型。
- 非基础造型继续严格区分，例如“白衣版”不会误用“黑衣版”。

## 生图前二次预检

- 单项“让 AI 生成”和“一键生成全部缺失图片”在调用生图模型前都会重新读取一次资产库。
- 二次检索命中时直接绑定已有图片并取消本次生图调用，避免页面异步匹配慢一步造成重复扣费。
- 人物、Canonical 场景和重要道具都使用同一套精确复用规则。

## 重复人物卡片修复

- 编剧或导演返回重复人物记录时，按“人物身份＋本集造型”合并。
- 已保存工程中的重复工作台卡片也会自动合并，优先保留有图片、已绑定资产库的记录。
- 不会自动删除用户资产库中的历史文件。
