# 漫镜 v1.5.2

v1.5.2 重点修复“已有资产仍被重复生成”的问题，并增强旧资产、跨剧集资产和人物多造型的复用能力。

## 已有资产直接采用

- 人物、Canonical 场景和重要道具在生图前都会重新读取完整资产库。
- 只要人物已经拥有可见图片，就直接视为可用资产；旧资源缺少 `sheetVersion` 不再触发重新生图。
- 当前集中的空占位框架不会遮挡其他剧集、旧项目或公共资产中的真实图片。
- 生图岗位重新运行时只处理真正没有图片的角色。

## 更灵活的历史资产识别

- 支持从 `identityKey`、`entityId`、人物标签和文件名识别同一资产。
- 兼容“人物标准图、角色参考图、角色设定图、三视图、四视图、Character Sheet、Turnaround”等旧命名。
- 可识别被旧版本归入“其他”类别、但带有人物/场景/道具语义标签的图片。
- 支持当前项目、公共资产及其他剧集/旧项目资产；当前项目和 Canonical 资产优先。

## 人物造型复用策略

- 优先选择身份和造型完全一致的图片。
- 找不到对应造型时，可回退使用同一人物已认可的图片，避免对同一人物重复生成。
- 严格匹配模式仍可区分白衣、黑衣等真实造型，底层测试覆盖不会误配的情况。

## 验证

- 资产复用专项测试：9/9 通过。
- Windows 生产构建通过。
