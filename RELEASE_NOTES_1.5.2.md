# 漫镜 v1.5.2 · 四区角色卡与连续性修复版

面向 AI 漫剧和系列视频制作的 Windows x64 桌面工作台。本次在 **保持版本号 1.5.2 不变** 的前提下，重新构建并替换安装包。

## 重点修复

- **只生成第一镜**：批准候选后自动定位下一条真正缺失的镜头；抽帧失败不再阻塞；增加“继续生成第 N 镜”兜底。
- **同人物跨版本变脸**：Canonical 人物身份仅在当前任务内绑定；每个角色只提交一份身份基准，禁止多个历史脸图混入同一请求；默认每 4 镜重新挂载。
- **人物角色卡升级**：固定“左侧 35%–40% 大正脸 + 右侧正面、45°侧面、背面全身三视图”，16:9、目标至少 1792×1024、纯白背景、均匀平光、轻微 T-pose。
- **多角度质量检查**：分别审核正脸、侧面轮廓、背面轮廓、五官、头身比和服装一致性；旧资产仍会直接复用，不因缺少新元数据重复扣费生成。
- **位置和道具漂移**：结构化提示词强约束左右位置、景深、朝向、手持关系和道具归属，每 6 镜重述空间基准；明确提示词不能绝对锁死生成结果。
- **镜头穿帮**：使用“稳定帧—单一动作—稳定帧”，复杂连续动作拆短，降低瞬移、复制和物体凭空出现/消失概率。
- **方舟引用 400**：仅在任务创建前明确拒绝视频/音频网址时，剔除无效引用并重试一次；人物、场景和道具图片继续保留，避免重复创建付费任务。

## 两种镜头继承模式

- **A · 身份优先（默认）**：抽取首尾帧只用于质检，不送入生成模型。变脸风险较低，但镜头衔接可能更跳。
- **B · 可控尾帧继承**：使用已批准上一镜的质检尾帧继承构图，同时叠加当前任务 Canonical 人物卡并声明身份优先。衔接更顺，但仍可能继承身份偏差。

两种模式都不会把参考图伪装成 Seedance `first_frame`。

## 实现边界

身份一致性和质检设计借鉴 Seedream 提示词指南、ConsistentID、ContextAnyone、GEN3C 和 VisionReward 的思路；**没有接入或运行这些外部模型**，底层视频生成引擎仍为用户配置的火山 Seedance。

## 验证

- 生产构建：通过
- TypeScript：通过
- ESLint：0 error（35 条 warning）
- 单元/集成测试：105/105 通过
- 打包程序冒烟：8/8 通过
- 安装包归档：7-Zip 验证通过（10 个文件）

## 下载

请下载 Release 中唯一的 `Manjing-1.5.2-Final-x64-Setup.exe`。

- 大小：116,639,687 字节（111.24 MiB）
- SHA256：`6A516281E795191E0B3981FEA96001FE1947086724460613323DBB94A5B511A5`
- Authenticode：`NotSigned`
