# 漫镜 v1.4.8

这是一次完整的 AI 漫剧生产能力升级，重点解决长剧人物与镜头一致性、Seedance 创建与恢复、资产复用、角色声音一致性，以及 Agent 团队的长期学习问题。

## 主要更新

### Agent 团队

- 总制片 Agent 统一拆解目标、分配任务、技能和记忆。
- 导演、编剧、分镜、视频、配音和剪辑 Agent 可独立聊天。
- 支持岗位长期记忆、技能编辑、资料导入和 MCP 学习沉淀。
- 支持删除单条聊天记录或清空指定 Agent 会话。

### Canonical 资产与一致性引擎

- 角色、造型、场景、道具和声音可设为 Canonical 标准资产。
- 已生成资产由 Agent 自动检索和复用。
- 新增人物、服装、场景、道具、空间、光线和镜头连续性评分。
- 上一镜结束状态自动传递到下一镜开始状态。
- AI 根据剧本语义命名资产，并自动补全角色身份与基础造型。
- 资产库支持预览、重命名、分类、批量选择和批量删除。

### 声音一致性

- 音频资产支持“所属角色”和“声音档案”。
- Seedance 生成时自动使用匹配角色的 `reference_audio`。
- 同一人物跨镜头保持音色、年龄感、语速和口音一致。
- 人物配音与背景音乐拆分为两个独立开关。
- 关闭人物配音后仍保留环境音和动作音效。

### Seedance 2.0

- 原生接入方舟多媒体任务接口。
- Seedance 2.0 Fast 自动使用 480p/720p，修复 1080p 参数错误。
- 创建等待提高到 180 秒，创建中断时不自动重复提交。
- 保存漫镜请求编号和方舟任务编号，查询与下载支持安全恢复。
- 收到有效 `video_url` 后立即回写镜头完成状态并进入下一岗位。
- 支持方舟可信人像 Asset ID（`asset://`）。
- 明确区分人像授权、内容审核、参数、权限、超时和网络错误。

### 视频生产逻辑

- 目标时长不超过 15 秒时优先一镜直出。
- 超过 15 秒时由 AI 根据剧情节奏判断镜头数量和不同时长。
- 逐镜生成后自动进入配音、音乐和剪辑流程。

## 安装与升级

1. 下载下方 `漫镜-1.4.8-可信人像修补-x64.exe`。
2. 关闭正在运行的漫镜后安装，可覆盖旧版本。
3. 本地项目、模型配置、Agent 记忆和资产不会因覆盖安装主动删除。
4. 使用真人参考素材时，请先在火山方舟完成可信人像授权并绑定 Asset ID。

## 说明

本版本为 Windows x64 免费内测版本。第三方模型调用可能产生费用；若 Seedance 创建连接中断，请先使用漫镜请求编号在方舟控制台确认是否已经产生任务，再决定是否重新提交。

## 安装包校验

```text
SHA256: 09F31E0B057390950BCD22DF01F0C6A18B69DCD03647BDD4C51CDBB14F8019DE
```

