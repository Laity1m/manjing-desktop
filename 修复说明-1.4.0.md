# 漫镜 1.4.0 修复说明

## 自主视频：独立 Agnes API

- “自主视频”新增独立的 `Agnes 自定义视频 API`，配置保存在 `videoLabConfig`，不再读取或同步 AI 工作台的视频岗位配置。
- Base URL 必须填写服务根地址，末尾不要带 `/v1`；输入时会自动清理 `/v1` 或 `/v1/videos`。
- 创建任务固定调用 `Base URL + /v1/videos`，不会错误拼接成 `/videos`。
- 使用 Agnes 原生异步流程创建任务并按 `video_id` 轮询结果。
- 默认模型为 `agnes-video-v2.0`，支持单张图片参考。

Agnes 示例 Base URL：

```text
https://apihub.agnes-ai.com
```

## AI 工作台：5% 卡顿修复

- 编剧与分镜阶段每 4 秒更新等待时间，进度会从 5% 平滑推进到 9%，不再表现为假死。
- 编剧、导演和剪辑云端请求等待上限调整为 60 秒。
- 编剧接口超时或异常时，自动生成本地基础分镜并继续后续生产，避免所有岗位永久等待上游交付。
- 活动记录会明确标注云端超时与本地保底，不会伪装成云端成功。

## 保留修复

- 火山方舟 Seedance 原生多媒体任务支持。
- Windows 导航卡死、启动白屏与软件渲染兼容修复。
