# 漫镜 v1.4.9 · 系列项目系统

`v1.4.9` 面向 20 集、40 集等连续短剧生产，新增“一个剧本一个项目”的系列制作体系。总剧本不再只是临时输入，而会被整理为可持续使用的项目知识、分集剧本和专属资产。

## 系列项目

- 用户可以创建、命名和删除独立系列项目。
- 支持导入 DOCX、PDF、TXT、Markdown、JSON、YAML 和 Skill 总剧本。
- 单份文档最大解析容量提高到 200 万字符。
- 自动识别中文数字或阿拉伯数字的分集标题。
- 自动建立剧集列表、制作状态和分集摘要。
- 旧版剪辑工程继续保留，不会因升级被转换或删除。

## 项目知识库

- 从总剧本提取人物介绍、人物关系、背景故事、世界规则和分集时间线。
- 新增可编辑的角色圣经。
- 新增可编辑、可锁定的项目长期记忆。
- 锁定记忆作为全剧硬约束，可演化记忆在制作过程中持续补充。

## 按集生成

- 用户可以选择任意一集进入 AI 工作台。
- 工作台自动接收项目名、集数、本集剧本和项目上下文。
- 每次只读取本集、相关角色、长期规则和上一集结束状态。
- 避免把 40 集全文一次性塞入模型导致上下文不足。
- 为后续跨集人物、服装、场景、道具和剧情状态一致性建立基础。

## 项目级资产

- 新生成资产自动记录 `projectId` 和 `episodeId`。
- 当前项目默认只能自动检索自身资产。
- 没有项目归属的历史资产继续作为公共资产使用。
- 用户明确设置为全局的资产可以跨项目复用。
- 不同剧本默认不会互相串用人物、场景和声音。

## 已包含的 1.4.8 能力

- Canonical 标准资产与一致性评分。
- 镜头结束状态向下一镜传递。
- 方舟可信人像 `asset://Asset ID`。
- Seedance 安全创建、状态恢复和下载重试。
- Seedance 2.0 Fast 分辨率适配。
- 人物声音资产和 `reference_audio` 复用。
- 人物配音与背景音乐独立开关。
- 资产语义命名、角色身份/造型补全和批量删除。

## 安装与升级

1. 下载 `漫镜-1.4.9-系列项目系统-x64.exe`。
2. 关闭正在运行的漫镜后覆盖安装。
3. 安装不会主动删除旧项目、资产、模型配置、技能和 Agent 记忆。
4. 打开“项目”页面，新建项目或导入总剧本即可使用系列制作功能。

## 安装包校验

```text
SHA256: 6D2C7F95F8CB857106115B9A072AD155F4E6C8F2873381F3E6B262FD696523A6
```

本版本为 Windows x64 免费内测版本。第三方模型调用可能产生费用，请以服务商计费规则为准。
