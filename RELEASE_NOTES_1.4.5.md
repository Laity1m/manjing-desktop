# 漫镜 1.4.5 记忆增强内测版

- 每个岗位 Agent 拥有独立聊天记录、技能和长期记忆。
- 新增经历、永久、复盘、自我认知和工作锚点五类记忆。
- 新增记忆重要度、来源、版本、使用次数、钉选、停用、归档和恢复。
- MCP、抖音和本地视频学习结果支持自动入库或候选审核。
- 相同来源内容自动去重，内容修改保留版本递增。
- 工作台按重要度和活跃度调用已审核内容，并记录实际使用次数。
- 原始学习内容保留在本机，索引和派生信息不替代原始内容。
- 本版本未引入 Ombre Brain 受限代码，记忆能力为漫镜自主实现。
- 为导演、编剧、分镜、视频、配音和剪辑 Agent 内置 24 项可执行岗位技能。
- 预设方法参考 ViMax、Pexo Skills、SmartSub、ZastTranslate 等 MIT 项目并重新编写，保留来源链接。
- 所有预设技能均可由用户编辑、停用、钉选、归档、恢复或删除。
- 修复 10 秒视频仍被强制拆成多个镜头的问题：目标不超过 15 秒时默认单镜直出。
- 超过 15 秒时才按模型单次上限拆镜，用户锁定的多镜分镜不受影响。
- 工作台单镜生成完成后允许保留最长 15 秒，不再强制回写为最多 10 秒。
- 技能库新增 .skill、Markdown、TXT、JSON、YAML、DOCX 和 PDF 本地导入。
- 每个文件作为一项完整技能保存，不再把技能正文显示为单行列表。
- 技能区升级为响应式卡片库，支持全文预览、重命名、正文编辑和来源查看。
- 对自动生成分镜增加硬性数量上限，10秒任务即使模型返回多镜也只接受1镜。
- 导演复核不得增加或拆分镜头；用户手动导入和锁定的多镜分镜仍完整保留。
- 撤销按总时长硬锁镜头数量，改由编剧分析剧情后建议镜头数、导演独立复核。
- 15秒仅作为单次生成技术上限；同一时空连续动作优先长镜头，确有叙事需要才拆镜。
